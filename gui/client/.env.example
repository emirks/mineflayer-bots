# URL of your local backend, exposed to the internet.
# Leave blank when running locally (frontend served by the same Node process).
#
# Options:
#   ngrok:            VITE_API_URL=https://xxxx.ngrok-free.app
#   Cloudflare Tunnel: VITE_API_URL=https://bots.yourdomain.com
#   Static IP / port forward: VITE_API_URL=http://YOUR_PUBLIC_IP:3030
#
VITE_API_URL=
