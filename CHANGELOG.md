# Changelog

## 2026-04-10
- **fix** `triggers/playerRadius.js` + `triggers/index.js` — panic watch no longer armed when bot is outside its base zone; root cause: during server maintenance teleports the bot is moved far from base, a player near the temporary location entered `alertRadius`, `fire()` correctly blocked the action chain via the base-zone guard in `triggers/index.js`, but `startPanicWatch()` was called unconditionally right after `fire()` — so when the server teleported the bot back a player in the region triggered an immediate emergency disconnect; fix: (1) `registerTrigger` now forwards `triggerConfig.baseZone` into `handlerOptions` so any handler can access it; (2) `playerRadius.js` destructures `baseZone` from options and checks `isInBaseZone()` before setting `alerted`, clearing the slow interval, or calling `startPanicWatch()` — outside the base zone the slow interval keeps running (detection continues) and the entire alert/panic pipeline is suppressed; blacklist path is unaffected

- **chore** `profiles/sentinel_2.js` — aligned triggers with `sentinel_1.js` (onSpawn `delayMs` 2s, `baseZone` radius 30, whitelist `Jynx_33` + `Raikuuru`, `sentinelSweep` maxRounds/interBlock timing); left distinct `username` and viewer port 3001 for the second bot

- **feat** `lib/skills/debugSpawnerWindow.js` (new) + `actions/debugSpawnerWindow.js` (new) + `actions/index.js` + `profiles/debug.js` — new `debugSpawnerWindow` skill and action: opens a spawner GUI, dumps full window debug info to the log: raw title JSON, parsed title text, window type/id/total-slot-count/container-slot-count, every occupied container slot (slot index, item name, count, metadata, truncated NBT), every occupied player-inventory slot in the same format, and a totals-by-item-type table sorted descending; `debug.js` profile now fires `debugSpawnerWindow` on spawn instead of `logSpawnerData`
- **feat** `lib/skills/debugSpawnerWindow.js` — refactored into `snapshotWindow` + `logSnapshot` helpers reused for both windows; added `probeConfirmSell` (default true) + `confirmSellSlot` (default 48) options: after logging the spawner window the skill clicks the gold ingot sell button via `bot.clickWindow`, waits for the `CONFIRM SELL` windowOpen event, logs that window in identical format, then closes it — the green confirm button is never clicked so nothing is sold; `actions/debugSpawnerWindow.js` passes new options through
- **feat** `lib/skills/sweepSpawnerPages.js` (new) + `actions/sweepSpawnerPages.js` (new) + `actions/index.js` + `profiles/debug.js` — new page-sweep skill: navigates to every spawner, opens its DonutSMP paginated GUI, and applies per-page rules: bones-only page → click dropper (slot 50) to drop, then click next-page arrow (slot 53) to advance; arrow-containing page → click sell (slot 48) to open CONFIRM SELL window, dynamically find `lime_stained_glass_pane` in the confirm window and click it to confirm, then break (no more pages after a sell); loops until no next-page arrow; does this for all spawners; logs page-by-page decisions + per-spawner and global summary; `debug.js` profile fires `debugSpawnerWindow` then `sweepSpawnerPages` on spawn
- **feat** `profiles/bone_collector.js` (new) — automated bone-farm harvester profile: `onSpawn` (5 s delay) + `onInterval` (10 min) both firing `boneSweep`; `playerRadius` panic safety at 6 blocks; SWEEP_OPTIONS constant shared between both trigger actions; viewer on port 3003
- **refactor** `actions/sweepSpawnerPages.js` → `actions/boneSweep.js` — renamed action to avoid confusion with other `sweep*` actions; updated log prefix to `[BONE-SWEEP]`; updated `actions/index.js` registry and `profiles/debug.js`
- **chore** `actions/logSpawnerData.js` — deleted (unused; replaced by `debugSpawnerWindow` in the debug profile); removed from `actions/index.js` registry
- **refactor** `lib/skills/spawnerWindow.js` — exported `waitForWindowOpen` so other skill files can share the correctly-cleaned-up listener pattern; updated `lib/skills/spawnerSurvey.js` to use `openSpawnerWindow` (removes the old inline buggy `new Promise` + `bot.once` that leaked listeners on timeout); updated `lib/skills/debugSpawnerWindow.js` to import `openSpawnerWindow` + `waitForWindowOpen` from `spawnerWindow.js` and removed its private stale `waitForWindow` copy; `lib/skills/index.js` now exports every skill in the catalogue (all four skill files) and serves as a living map of available spawner skills
- **refactor** `lib/skills/spawnerWindow.js` (new) + `lib/skills/sweepSpawnerPages.js` + `actions/sweepSpawnerPages.js` — decomposed the monolithic sweep into proper skill layers following mindcraft's atomic-skill pattern: `spawnerWindow.js` now owns five single-responsibility skills (`classifySpawnerPage` pure fn, `openSpawnerWindow`, `dropSpawnerPage`, `navigateToNextPage`, `sellSpawnerPage` — each does exactly one server interaction with simple positional params); `sweepSpawnerPages.js` uses those atomic skills in `sweepOneSpawner` (one spawner, full page loop, returns `droppedCounts`/`soldCounts`) and `sweepSpawnerPages` (finds all spawners, navigates, calls sweepOneSpawner); action now holds all orchestration logic, receives structured results, logs per-item grand totals for both dropped and sold, and computes money when a `prices` map is configured (`prices: { arrow: 2.5 }` → `arrow×384 @2.5/ea = $960.00` + `TOTAL MONEY` line)
- **fix** `lib/skills/sweepSpawnerPages.js` — complete rewrite of window-update logic: root cause was DonutSMP sending `close_window` + `open_window` (new ID) for page navigation, not `window_items` on the same window; `setWindowItems:${oldId}` never fired → stale `win` reference with old slot data → slot 53 always appeared to have an arrow → infinite loop; fix: listen for `windowOpen` (guaranteed by mineflayer's `prepareWindow` to fire only after all slot data is loaded) for page transitions, using a properly-cleaned-up one-shot listener (settled flag + explicit removeListener on timeout) to eliminate the MaxListenersExceededWarning; dropper click now uses a fixed `dropSettleMs` (400ms) settle delay instead of event-based waiting; confirm sell now uses right-click (`mouseButton=1`) + `confirmSettleMs` (600ms) settle; skill option renamed from `timeoutMs` → `winTimeoutMs` and profile options updated to prevent the action executor from treating the window timeout as an action-level kill timer


- **fix** `actions/dropItems.js` — added `settleMs` (default 500 ms) post-drop wait before returning; root cause: mineflayer's `clickWindow` for normal inventory slots calls `waitForWindowUpdate` which returns immediately (no server ACK), so consecutive `window_click` packets for multi-stack drops all carry the same stale `stateId`; on DonutSMP (Paper 1.21.x) the server sends corrective `set_slot` resync packets when it sees a stale stateId, which can revert un-confirmed drops — `settleMs` covers 2–3 RTTs so all resync packets arrive before `disconnect` fires `bot.quit()`; added post-settle inventory check that warns if items remain after the settle

- **refactor** `actions/sentinelSweep.js` — complete stability rewrite: (1) primary termination is now world-scan returning 0 blocks (chunk cache, fully local) instead of inventory count; ghost-block guard does one betweenSweepsMs wait + rescan before accepting world-clear; (2) `betweenSweepsMs` sleep only applied when `blocks.length <= 2` — when more blocks remain, travelling to the next one already covers the ghost window; (3) sneak-sync gap: `sleep(sneakSyncMs=60ms)` added after `setControlState('sneak', true)` and before `breakBlockAt`, ensuring the sneak packet (batched into the next ~50ms physics tick) reaches the server before the dig START packet; (4) inventory check moved to a dedicated post-exit verification phase: if collected < totalExpected after world-clear, loop rescans world and inventory every `verifyPollMs` for up to `verifyTimeoutMs`, re-breaking any reappeared blocks, until fully satisfied or timeout; (5) added inline comments documenting the interaction type (local/physics-tick/round-trip) of every primitive used in the sweep

- **feat** `package.json` + `scripts/zip-dist.js` — `pnpm build` now also runs pkg for `node18-linux-x64` → `dist/sentinel`; postbuild produces `dist/sentinel-v<version>-linux-x64.zip` (`sentinel` binary + `profiles/`) alongside the existing Windows `sentinel-v<version>.zip`; chmod 0o755 applied to the Linux binary before zipping when supported

## 2026-04-09
- **fix** `lib/world.js` — removed `getSpawnerStackCount` and `getSpawnerInfo`; both sent packets to the server (`bot.activateBlock` + awaiting `windowOpen`) which violates world.js contract of passive, already-loaded-chunk reads only; moved to `lib/skills/spawnerSurvey.js` where active server interactions belong
- **feat** `lib/skills/spawnerSurvey.js` (new) + `lib/skills/index.js` — `getSpawnerInfo(bot, block, ammoItems, timeoutMs)`: right-clicks a spawner, waits for `windowOpen`, reads stack count from DonutSMP NBT title + ammo counts from `win.containerItems()`, closes window; returns `{ stackCount, ammo }` or `null` on timeout; `surveySpawners(bot, options)`: finds all spawner blocks within `radius` (default 64), navigates to each via `skills.goToPosition`, calls `getSpawnerInfo` per block, logs per-spawner line `(x,y,z) dist:Xm stacks:N bone:M arrow:P` and totals summary; returns structured `[{ block, pos, distAtScan, stackCount, ammo }]` array; `ammoItems` defaults to `['bone', 'arrow']`; both `getSpawnerInfo` and `DEFAULT_AMMO_ITEMS` exported alongside `surveySpawners`; note: import directly via `require('../lib/skills/spawnerSurvey')` until `skills.js` migration
- **refactor** `actions/logSpawnerData.js` — updated import from `world.getSpawnerStackCount` to `getSpawnerInfo` from `lib/skills/spawnerSurvey`; now logs both stack count and ammo from the single `{ stackCount, ammo }` return value

## 2026-04-09
- **feat** `triggers/playerRadius.js` + `actions/surveySpawners.js` + `actions/sentinelSweep.js` — survey abort on alert: `playerRadius` sets `bot._sweepPending = true` the moment a normal alert fires; `surveySpawners` action checks this flag before each `goToPosition` and after (two checkpoints per spawner) and breaks out immediately, saving partial data; `sentinelSweep` clears `bot._sweepPending` at start so flag never bleeds into post-reconnect surveys; survey loop inlined in the action (was one `surveySpawners()` call) so checkpoints can be inserted — partial cached data is still useful for inventory verification
- **feat** `actions/surveySpawners.js` (new) + `actions/index.js` — new `surveySpawners` action: calls `surveySpawners()` skill (navigate + GUI open per spawner), saves `{ timestamp, results, totalExpected }` to `bot._spawnerSurvey`; registered in action registry
- **refactor** `actions/sentinelSweep.js` — removed per-block `getSpawnerInfo()` call (survey no longer happens during the sweep); at alert time, reads pre-cached `bot._spawnerSurvey.totalExpected` set by the `surveySpawners` action; sweep is now a pure break-loop with no GUI interactions under pressure; logs a warning if no survey cache is present
- **refactor** `profiles/sentinel_1.js` + `profiles/sentinel_2.js` — replaced single `onInterval`+`logSurroundings` trigger with two new triggers: `onSpawn`(delayMs:10000) and `onInterval`(5min) both firing `surveySpawners`; bot now navigates to spawners 10 s after spawn (pre-positions near cluster) and refreshes survey every 5 min
- **refactor** `actions/sentinelSweep.js` + `profiles/sentinel_1.js` + `profiles/sentinel_2.js` — rewrote sweep algorithm to round-robin strategy: bot sweeps ALL spawner positions each round without waiting between blocks (navigation to other blocks naturally covers each ghost-window); `ghostBlockWaitMs` (1500ms) is applied only when exactly one spawner position remains, since that is the sole case where nothing else fills the wait; removed `blockDelayMinMs`/`blockDelayMaxMs` (obsolete); integrated `getSpawnerInfo()` from `lib/skills/spawnerSurvey` on first visit to each position to record stack count; added inventory verification after sweep (collected vs surveyed total) with `pickupFloorDrops` fallback; added `verifyInventory` gate: disconnect only proceeds after confirming all spawners are in inventory (except panic)
- **refactor** `actions/breakAllBlocks.js` → `actions/sentinelSweep.js` + `actions/index.js` + `profiles/sentinel_1.js` + `profiles/sentinel_2.js` — renamed action from generic `breakAllBlocks` to `sentinelSweep`; the action IS sentinel's core asset-denial response (navigate to every spawner in range, sneak-break each, re-scan in rounds until area is clear) and is not shared with any other profile; log prefix changed from `[ACTION]` to `[SENTINEL]` to reflect ownership; `breakAllBlocks.js` deleted
- **feat** `actions/logSpawnerData.js` (new) + `actions/index.js` — one-shot diagnostic action: finds nearest spawner within `radius` blocks, dumps full `block.blockEntity` NBT to log, then logs every entity within `entityRadius` blocks (default 8) of the spawner (id, type, name, displayName, `metadata[2]` CustomName + `metadata[3]` CustomNameVisible, position); supports `waitMs` option to delay the scan so hologram entities have time to load after spawn; replaced deprecated `entity.objectType` with `entity.displayName`
- **feat** `lib/world.js` — `getSpawnerStackCount(bot, block, timeoutMs)`: opens the spawner GUI via `bot.activateBlock()`, awaits `windowOpen`, extracts plain text from the prismarine-nbt compound title (`title.value.text.value`), parses the leading integer (e.g. `"2 ѕᴋᴇʟᴇᴛᴏɴ ѕᴘᴀᴡɴᴇʀѕ"` → `2`), closes the window; returns `number|null`; exported from `world.js`
- **refactor** `actions/logSpawnerData.js` — now a thin diagnostic wrapper over `world.getSpawnerStackCount()`; confirmed working: title format is prismarine-nbt compound, stack count is at `title.value.text.value`
- **feat** `profiles/debug.js` — removed `waitMs`/`entityRadius` (no longer needed); probe fires immediately at spawn

## 2026-04-09
- **chore** `package.json` + `scripts/zip-dist.js` — packaged exe renamed to `dist/sentinel.exe`; zip artifact is `dist/sentinel-v<version>.zip` with `sentinel.exe` at archive root

## 2026-04-08
- **feat** `triggers/onInterval.js` (new) + `triggers/index.js` — new `onInterval` trigger type: fires the action stack on a recurring `intervalMs` schedule (unlike `onSpawn` which fires once); returns `{ cancel }` for clean session teardown; registered as `onInterval` in the trigger registry
- **feat** `actions/logSurroundings.js` (new) + `actions/index.js` — new `logSurroundings` action: calls `world.getNearestBlocks()` for each configured block type within `radius`, logs count + sorted position/distance table; diffs against `bot._surroundingsState[key]` (keyed by block names + radius, reset on reconnect) and logs appeared/removed entries; first call logs "baseline recorded"; state is a plain-object array (no Vec3 references)
- **feat** `profiles/sentinel.js` + `profiles/sentinel2.js` — added `onInterval` (5 min, priority -1) trigger with `logSurroundings` action scanning for `spawner` within 64 blocks; priority -1 ensures any `playerRadius` defensive chain always preempts the scan
- **feat** `profiles/sentinel2.js` — added `baseZone: { radius: 50 }` to `playerRadius` trigger (radius 50 > alertRadius 30 so the trigger can still fire, while still guarding against acting after being warped off the island)
- **fix** `lib/createBotSession.js` + `profiles/_base.js` — health heartbeat: replaced `setInterval` with recursive `setTimeout` + configurable `jitterMs` (default ±30 s) so the cadence is not perfectly mechanical; added `bot.health <= 0` detection that calls `bot.quit()` **without** setting `bot._quitting` so BotManager sees `intentional:false` and reconnects with normal backoff; renamed `healthInterval` → `healthTimer` (now a timeout handle); cleanup uses `clearTimeout`
- **feat** `lib/createBotSession.js` + `profiles/_base.js` — added always-on health heartbeat: every 5 min (configurable via `profile.healthCheck.intervalMs`) logs `[HEALTH] up Xm | hp:Y food:Z | pos:(…) | online players: N` to `session.log`, then sends `/ping` whose server reply appears as `[CHAT]` — gives passive process-alive proof + active end-to-end TCP liveness confirmation with zero anti-cheat risk; `healthCheck.pingCommand: null` disables the command; `healthCheck.enabled: false` disables the feature entirely; interval is cleared on `kicked` and `end` alongside the existing snapshot interval
- **feat** `triggers/playerRadius.js` — distance logging is now change-only and position-based; a `prevPos` map (username → `"x,y,z"` at 2 dp) suppresses identical ticks and correctly detects circular movement where distance stays constant; log line now includes exact XYZ coords; players that leave detection range emit `→ out of detection area`
- **feat** `triggers/playerRadius.js` — added `whitelist` and `blacklist` options (arrays of usernames, case-insensitive); whitelisted players are only ever logged with `[WL]` tag and never trigger alert or panic; blacklisted players cause immediate panic at `alertRadius` (bypasses action queue); normal (unlisted) players keep existing alert→action→panic flow; shared `triggerPanic()` helper deduplicates the emergency-quit path
- **feat** `profiles/sentinel.js` — added `whitelist: []` and `blacklist: []` fields to `playerRadius` options with inline documentation
- **feat** `orchestrator.js` — CLI now shows an interactive profile picker when run with no arguments; scans `profiles/` (skips `_base.js`), reads each file's leading comment as a description, prints a numbered list, and accepts space/comma-separated numbers or `"all"`; argv path still works unchanged; no new dependencies (uses Node built-ins `fs` + `readline`)
- **fix** `lib/logger.js` — `LOGS_BASE` now uses `path.dirname(process.execPath)` when `process.pkg` is truthy; fixes "Cannot mkdir in a snapshot" crash when running the packaged exe
- **feat** `lib/createBotSession.js` — when `process.pkg` is true, loads the profile from the real filesystem next to the exe (`<exeDir>/<profileName>.js`) instead of the read-only snapshot; allows the friend to edit the profile in Notepad without rebuilding
- **refactor** `launch-sentinel.js` → `launch.js` + `orchestrator.js` — launcher is now generic; `runCLI()` extracted from the `require.main` guard and exported; `launch.js` calls `runCLI()` directly (bypassing the guard); `listProfiles()` reads from `path.dirname(process.execPath)` when `process.pkg` so profile picker works from the real filesystem next to the exe; build target changed to `dist/bot.exe`; `launch-sentinel.js` deleted
- **chore** `scripts/zip-dist.js` (new) + `package.json` — `postbuild` now also runs `zip-dist.js` after copying profiles; produces `dist/bot-v<version>.zip` containing `bot.exe` + `profiles/` (~48 MB); excludes `auth-cache/` and `logs/`; uses `archiver` (new devDependency)
- **chore** `scripts/copy-dist-profiles.js` + `orchestrator.js` + `lib/createBotSession.js` — profiles now live in `dist/profiles/` instead of `dist/`; `postbuild` copies every `.js` from `profiles/` to `dist/profiles/`; `listProfiles()` and profile loader both resolve to `<exeDir>/profiles/` when `process.pkg`
- **chore** `package.json` + `launch-sentinel.js` (new) — added `@yao-pkg/pkg` dev dependency; `pkg` config bundles all profiles/triggers/actions/lib + minecraft-data assets; `pnpm build` produces `dist/sentinel-bot.exe` (Node 18 embedded, ~76 MB); `launch-sentinel.js` calls `spawnBot` directly (not via argv) because `require.main === module` is false inside a pkg exe; `pnpm.overrides.into-stream: 6` fixes ESM crash on Node 20
- **feat** `lib/createBotSession.js` — records `bot._base = bot.entity.position.clone()` on first spawn so triggers can reference the bot's home position
- **feat** `triggers/index.js` — `fire()` now checks `triggerConfig.baseZone` before queuing an action chain; if the bot is farther than `baseZone.radius` blocks from `bot._base` the chain is silently skipped (sensing keeps running); distance is logged at info level for visibility
- **feat** `profiles/sentinel.js` — added `baseZone: { radius: 30 }` to the `playerRadius` trigger; sentinel actions only execute when within 30 blocks of spawn; logs `[TRIGGER] skipped — Xm from base` when outside
- **feat** `lib/logger.js` — changed log directory layout to `logs/<profile>/<YYYY-MM-DD>/run_<N>/session.log`; run number auto-increments per process start (reconnects share the same run dir); exposed `log.runDir` on the logger object; added `createSnapshotWriter(runDir)` export that opens `snapshots.jsonl` (NDJSON, append-only) in the same run directory
- **feat** `lib/snapshot.js` (new) — extracted `buildSnapshot(bot)` into its own module; extends snapshot with: look direction (yaw/pitch), velocity, gameMode, biome, heldItem, armor slots (helmet/chest/leggings/boots), surroundings (below/legs/head block names), nearby blocks with exact positions within 8 blocks (nearest 20 non-air via `bot.findBlocks`); all floats rounded (health 1dp, look 2dp, vel 3dp); all risky calls wrapped in try/catch
- **refactor** `lib/createBotSession.js` — removed inline `buildSnapshot`; now imports from `lib/snapshot.js`; starts a 1-second `setInterval` after `spawn` writing to `snapshots.jsonl`; interval cleared on `end` and `kicked`
- **feat** `lib/createBotSession.js` — added `bot.on('message')` listener; logs every incoming in-game chat/system message as `[CHAT] <text>` via the session logger (console + `session.log`)

## 2026-04-07 (per-profile account isolation)
- **refactor** `profiles/_base.js` — removed `username` and `profilesFolder` from `bot`; these have no sensible shared default and must be per-profile so concurrent bots never share an account (server blocks duplicate logins); updated comment to document the required pattern
- **refactor** `profiles/sentinel.js` — added explicit `bot: { ...base.bot, username, profilesFolder: './auth-cache/sentinel' }`
- **refactor** `profiles/trader.js` — same; `username` left blank (different account required for concurrent run with sentinel)
- **refactor** `profiles/debug.js` — same; `username` left blank with note that it can share sentinel's account if not running concurrently

## 2026-04-07 (logging system)
- **feat** `lib/logger.js` (new) — `createLogger(name)` factory; module-level registry so BotManager and createBotSession share one file stream per profile; per-bot output to `logs/<name>.log` (plain text, append); coloured console output with ANSI (no deps); bot name tag colour-coded from a pool for multi-bot terminal readability; `sessionMark(label)` writes a visual separator in file and console at every connect attempt; no external dependencies
- **feat** `lib/createBotSession.js` — creates `bot.log = createLogger(profileName)` before any log output; spawn event now logs a state snapshot (position, health, food); all bare `console.*` calls replaced with `bot.log.*`
- **feat** `lib/BotManager.js` — creates `this.log = createLogger(profileName)` in constructor (same registry instance as session); calls `this.log.sessionMark(...)` at start of each connect attempt so reconnect cycles are clearly delimited in the log file; `_setState` logs enriched state transition line (prev→next, uptime, attempt); all bare `console.*` replaced with `this.log.*`; added `_fmtUptime` helper
- **refactor** `triggers/index.js`, `triggers/playerRadius.js`, `triggers/blockNearby.js`, `triggers/onSpawn.js` — all `console.*` replaced with `bot.log.*` at appropriate levels (info/warn/error)
- **refactor** `actions/index.js`, `actions/breakAllBlocks.js`, `actions/breakBlock.js`, `actions/disconnect.js`, `actions/dropItems.js`, `actions/goToBlock.js`, `actions/pickupItems.js`, `actions/sendChat.js`, `actions/takeFromChest.js`, `actions/startDebugScan.js` — all `console.*` replaced with `bot.log.*`
- **docs** `DEVELOPMENT.md` — logging todo items marked complete; added remaining items (in-game chat log, periodic snapshots)

## 2026-04-07 (GUI dashboard)
- **feat** `gui/server.js` (new) — Express + Socket.io server; serves dashboard; REST API for instance CRUD (create/update/delete/start/stop) and profile code read/write; forwards all EventBus events to connected browser sockets; ring-buffer log of last 200 entries sent to new connections; SIGINT stops all bots gracefully
- **feat** `gui/public/index.html` + `style.css` + `app.js` (new) — browser dashboard: bot grid with per-instance state badges (idle/connecting/connected/disconnected/reconnecting/stopped/failed), live uptime counter, Connect/Stop/Edit buttons; Add Bot modal with settings form + Profile Code editor tab (reads/writes .js profile files, clears require cache on save); event log panel; fully dark-themed (GitHub-dark palette)
- **feat** `instances.json` (new) — persisted bot instance configs (label, profile template, username, host, port, auth, viewerPort, reconnect); each instance gets its own auth-cache subfolder for account isolation
- **feat** `orchestrator.js` — added `spawnInstance(instance)`: merges per-instance overrides (username, host, viewerPort) onto the named profile template; sets `profilesFolder: ./auth-cache/<safeUser>` per account; uses instance.id as the BotManager map key; exported in module.exports
- **fix**  `lib/BotManager.js` — added `profileConfig` field; `createBotSession` now called with `profileConfig || profileName`; `CONNECTED` state now fires on `bot.once('login')` (previously fired immediately after TCP connect, before the server confirmed login); `_attempt` counter reset on successful login
- **fix**  `lib/createBotSession.js` — parameter renamed to `profileNameOrConfig`; accepts a string (loads profile file as before) or a pre-built config object (used by `spawnInstance` for GUI instances); `profileName` derived from `_instanceId` / `_profileTemplate` for log labels
- **chore** `package.json` — added `express ^5.2.1`, `socket.io ^4.8.3`; added `"gui": "node gui/server.js"` script

## 2026-04-07 (documentation sync)
- **docs** `ARCHITECTURE.md` — full snapshot update: corrected bot.js box (now thin wrapper), updated triggers/index.js box (createTriggerRegistry factory + priority queue), updated playerRadius/blockNearby/onSpawn with `returns { cancel }` and panic-path detail, updated lib/skills.js to show `blockDelay(bot)` replacing runtimeConfig, updated lib/runtimeConfig.js box to LEGACY status, added lib/createBotSession.js / lib/BotManager.js / lib/EventBus.js / orchestrator.js boxes to §1 YOUR CODE, rewrote §2 Boot Sequence with dual paths (bot.js + orchestrator.js), rewrote §3 Trigger→Action Data Flow to show priority queue and emergency path, updated §6 package table loader column, updated §7 run commands, rewrote §8 Extending with cancel pattern + EventBus example, added §9 Orchestration Layer (isolation table, BotManager state diagram, EventBus event catalogue, future GUI integration pattern)
- **docs** `.cursor/rules/mineflayer-bots.mdc` — added ARCHITECTURE.md maintenance contract: add/update/remove in sync with code; never strip accurate detail; changelog vs snapshot discipline explained

## 2026-04-07 (multi-bot orchestration)
- **arch** `lib/createBotSession.js` (new) — isolated bot session factory; stores profile on `bot._config`; calls `createTriggerRegistry()` per session; returns `{ bot, promise }` without calling `process.exit()`; used by both `bot.js` and `BotManager`
- **arch** `lib/BotManager.js` (new) — per-profile lifecycle manager; state machine (IDLE→CONNECTING→CONNECTED→DISCONNECTED→RECONNECTING→STOPPED→FAILED); exponential-backoff reconnect loop; exposes `start()`/`stop()`/`getSnapshot()`; emits `stateChange`, `reconnecting`, `error`
- **arch** `orchestrator.js` (new) — multi-bot entry point; manages a `Map<profileName, BotManager>`; exposes `spawnBot()`, `stopBot()`, `getBotStates()`; forwards all manager events to EventBus; handles `SIGINT` gracefully; dual-use (CLI + importable module for future GUI)
- **arch** `lib/EventBus.js` (new) — singleton `EventEmitter` for cross-bot coordination; bots publish game events; orchestrator and future GUI subscribe; decouples bot sessions from each other
- **fix**  `lib/skills.js` — removed `runtimeConfig` singleton import and module-level `blockPlaceDelay` constant; replaced with `blockDelay(bot)` helper that reads `bot._config` per-call; fixes multi-bot collision where second bot's config overwrote first's
- **arch** `triggers/index.js` — rewritten as `createTriggerRegistry()` factory; each bot session gets its own isolated priority queue and cleanup handles; priority queue sorts by `triggerConfig.priority` (default 0, higher runs first among queued chains, re-sorted before each dequeue); trigger handlers now return `{ cancel() }` for clean session teardown
- **fix**  `triggers/playerRadius.js`, `triggers/blockNearby.js`, `triggers/onSpawn.js` — all return `{ cancel() }` so `stopAll()` can clear their intervals/timers on session end; playerRadius hoists `fastInterval` to outer scope for reliable cleanup
- **refactor** `bot.js` — reduced to a thin wrapper over `createBotSession`; all session logic moved to `lib/createBotSession.js`; single-bot usage unchanged (`node bot.js <profile>`)

## 2026-04-07 (remaining stability fixes)
- **docs** `DEVELOPMENT.md` — feature todos use nested `- [ ]` task lists; completed truncated priority-fix checklist (runtimeConfig, panic race, panicRadius:0, trigger cleanup API)
- **feat** `profiles/sentinel.js`, `profiles/trader.js`, `profiles/debug.js` — added `timeoutMs` to every action that can block (breakAllBlocks: 300s, takeFromChest: 60s, pickupItems: 30s, dropItems: 15s, sendChat: 4–5s); startDebugScan and disconnect have no timeout since they return/exit immediately
- **fix**  `triggers/playerRadius.js` — added `panicRadius <= 0` early-return guard in `startPanicWatch()`; previously the interval ran forever doing nothing when panicRadius was 0 (condition `distanceTo < 0` is never true)
- **fix**  `triggers/playerRadius.js`, `actions/disconnect.js` — call `bot.pathfinder.stop()` (with guard) before `bot.quit()` so any in-flight `pathfinder.goto()` rejects immediately rather than waiting for the socket-close error to propagate; makes the action-chain abort faster and cleaner

## 2026-04-07 (stability fixes)
- **fix**  `bot.js` — `bot.on('spawn')` → `bot.once('spawn')`; prevents triggers being re-registered on every dimension change (mineflayer re-emits `spawn` on `respawn` packets from `/warp`, `/skyblock`, etc.), which caused duplicate polling intervals and double action stack executions
- **fix**  `triggers/index.js` — added module-level `actionChain` promise queue; all `fire()` calls append to the tail via `.then()` so action stacks from different triggers are serialised through a single queue; trigger polling intervals remain fully parallel; panic `bot.quit()` in `playerRadius` bypasses the queue intentionally
- **fix**  `triggers/playerRadius.js`, `actions/disconnect.js` — set `bot._quitting = true` before `bot.quit()` so the action executor can detect a closing connection and abort the chain cleanly instead of issuing commands to a dead socket
- **fix**  `actions/index.js` — `executeActions` now checks `bot._quitting` at each step and breaks early; accepts `context` as a third arg (trigger data forwarded from `fire()`); supports optional `opts.timeoutMs` per action via `Promise.race` to prevent a stuck pathfinder from hanging the entire chain indefinitely
- **arch** `ARCHITECTURE.md` — updated triggers/actions sections, boot sequence, data-flow diagram, and Extending guide to reflect all five stability fixes above

## 2026-04-07
- **arch** Added `ARCHITECTURE.md` — full system map (server→proxy→mc-protocol→mineflayer→your code), TCP pipeline, mineflayer plugin list, world-interaction table, velocity bug analysis
- **arch** Added compact system map + `ARCHITECTURE.md` reference to `.cursor/rules/mineflayer-bots.mdc`
- **fix**  `lib/velocityPatch.js` — `prependListener` on `entity_velocity` + `spawn_entity`; copies `packet.velocity.{x,y,z}` → flat `velocityX/Y/Z` before mineflayer `entities.js` runs; fixes NaN position corruption on DonutSMP (1.20.4 via ViaVersion)
- **feat** `profiles/sentinel.js` — `playerRadius` trigger: alert@3 blocks, panic@0; action stack: `breakAllBlocks(spawner, r=64)` → `dropItems(spawner)` → `disconnect`
- **feat** `profiles/trader.js` — `onSpawn` → `/warp market`; `blockNearby(chest)` → loot → `/sell all` → `pickupItems`; `playerRadius` panic@5 → `disconnect`
- **feat** `actions/breakAllBlocks.js` — multi-round rescan loop, random 400–1600 ms human delay between blocks, sneak during dig, `maxRounds` safety cap
- **feat** `lib/protocolDebug.js` — opt-in packet tracer; hooks `bot._client` for parsed/raw IN and OUT; streams to `logs/`; `MC_PROTOCOL_DEBUG=1` env override
